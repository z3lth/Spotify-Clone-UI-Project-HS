import 'package:flutter/material.dart';

class cards {
  final String id;
  final String title;
  final Color color;

  const cards({
    @required this.id,
    @required this.title,
    @required this.color,
    Type child,
  });
}
